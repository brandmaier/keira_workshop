# Die Item Schwierigkeit lässt sich mit folgendem Befehl ausgeben:

keira::plot_item_difficulty(eval_file = "nops_eval_fake.csv")

# Die Verteilung aller erreichten Punktzahlen lässt sich so darstellen

keira::plot_points_distribution(eval_file = "nops_eval_fake.csv")
